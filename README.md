# Stannum
A mostly simple language, using a tree-walking interpreter, with hopefully more to come.

```
greet = (name) {
    write("Hello, ");
    write(name);
    writeline("!");
}

main = () {
    name = "World";
    greet(name);
}
```
